# Native-palette Combo participation graphics

Candidate `a28b624bb13c8f2f2597a4d4bd3999b17c234b99` remains unchanged. E01 stays
accepted; E02-E05 remain open. This checkpoint adds an actual new-body Combo
participant test and a proven hidden-constructor observer, not a ROM change.

## Actual chain and native-donor control

`test-art-native-combo-participation` reuses the fresh native-palette four-class
ready allocation at `build/art/native-palette-entry/20260919T070815.647048Z/`.
Generic Dark Knight117 is the actual active unit; generic Viking118 is already
allocated with its own new body. Neither job nor actor identity is rewritten.
The Dark Knight initiates an original native racial Combo; the Viking has the
new Tempest Combo. Both have declared legal weapons, mastered assignments and
three JP. Other members have no assigned Combo and zero JP. Starting positions,
target HP500 and native RNG0 are explicit disposable inputs.

The initiator starts at1,14 and moves natively to4,14. The Viking starts at5,13,
adjacent to the target at5,14. This respects Tempest Combo's participation range1.
The native-donor control changes exactly the Viking lesson's two-byte global
profile to native donor7, matching existing original-profile evidence. Its
actor graphs, native menus, chain selection, hit and JP code are untouched.

Read-only observation at080B32E8 captures the native published membership:
initiator02000290, target020033E4, sole participant02000398. No trap ROM, simulated
membership or post-allocation job rewrite is used. Both branches produce55
damage. Initiator JP changes3 to0; participant JP remains3, matching native
behavior. Equipment/mastery outcomes also match the native-donor control, class
identities survive, action roots retire and the next native turn returns.

Both bodies are checked at each of1208 action frames per branch, plus ready and
confirmation. Every sample requires its owned resource and declared native
sequence, bounded tile/OAM allocation, and a directly verified or strictly
bounded retained upload. Visible non-idle modes include58/62/74/84/86/87 for the
Dark Knight and69/73/85 for the Viking. At every action frame, all1024 hardware
OAM bytes, including palette-bank and affine fields, equal the independently
reconstructed original native producer at its actual composition boundary.
Displayed palette bytes also match that boundary; inactive custom tags are not
used. These runs use the established read-only host observer; they do not claim
a separate whole-state/frame observer-equivalence experiment for this action.

Runtime `test-art-native-combo-participation` passes21801 checks in terminal
runner `20260919T073015.399691Z`. Report:
`build/art/native-combo-participation/20260919T073016.151723Z/report.json`.
The reports and exact retained inputs are pinned in
`native-art-native-combo-evidence.json`.

This closes the missing rendered participation consumer for this representative
Viking chain with two new generic bodies. It does not prove every new class's
participation/casting/secondary action, auxiliary effect art, every action-tail
frame, cold save or final artwork. Existing all-ten solo Fight/Combo and gameplay
participant evidence remain separately scoped and reusable.

## Retained failure and independently proven observer correction

Runner `20260919T072442.211336Z` failed at action373. Full artifacts remain at
`build/art/native-combo-participation/20260919T072442.986387Z/`.
The native membership list was empty because the fixture placed the Viking two
cells away. The corrected one-cell position follows the approved/native range;
neither chance nor eligibility is overridden and RNG0 is retained.

The same failed frame also shows a legitimate native constructor reset while
the Combo banner hides both body allocations. Original visible-reset evidence
required a displayed copy, so it correctly did not establish this different
case. `native_hidden_constructor.py` handles only an initial45/65 reset within
four frames of a direct upload: zero index/timer, missing live tile/layout,
exact current descriptor/count, identical allocation/queued layout and unchanged
complete previous pixels. It additionally proves all original native OAM bytes
and that no hardware object references any retained body tile, including8bpp
objects. It labels the pixels as retained and hidden, never a displayed new pose.

`test-art-native-hidden-constructor` passes36 checks in terminal runner
`20260919T072941.306526Z`, report
`build/art/native-hidden-constructor/20260919T072942.071942Z/report.json`.
The actual original21618 constructor, with both incoming stack residues,
reproduces all72 captured actor bytes from a controlled clone with recorded prior
actor fields. This is not reconstruction of the preceding whole-game state.
Negative field, pixel, expired-anchor and changed-hardware cases are rejected.

The generic original-producer/tile-overlap proof is factored as
`native_oam_evidence.native_tiles_hidden`. The existing custom-overlay-specific
checks remain in `hidden_body`; `test-art-native-only-oam` still passes793 checks
in the same terminal runner, report
`build/art/native-only-oam/20260919T072942.224097Z/report.json`.
The corrected successful participation case does not hit the hidden-constructor
branch; its positive coverage is the retained native constructor experiment,
not a fabricated claim about the successful chain's frames.

## Next E02 work

Reconcile the remaining casting/secondary/effect/UI consumers against the native
shared-palette architecture. Reuse unchanged action-graph and gameplay evidence;
use the fresh own-ROM state where a runtime consumer is genuinely missing.
Do not reenable custom palette observers or derive display from stale tags.
E03 demanding lifetimes, E04 campaign/save relevance and E05 review/clean build/
independent-save packaging remain. No installed build, save or ROM is changed.
