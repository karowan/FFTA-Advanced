# Council: paged native job wheel and temporary reaction guard

2026-09-14. Bounded independent source/Thumb/Unicorn review. Accepted test input: job-ui SHA-1 `2d39c3fb6f0076df976cebc98bf3ed14c180bd85`, engine `3745b440` prefix, reconstructed action-data baseline `d81b8966b7373029b4e2fbd9a6c14d11b5603cc7`. Exact input and original manifest are frozen at `build/expansion/probes/job-wheel-input.gba/json`. Helper addresses are resolved from those installed hook bytes, not mutable engine symbols. No engine files were edited.

## Verdict

**Accept the current scoped paging, donor wrappers, and inactive-reaction guard.** `scripts/test-job-wheel.py` passes28,226 assertions,17,280 completed function invocations, and additional actual inline-hook/native-row segments. It records14,448 correctly aligned C-entry visits. Results: `build/expansion/probes/job-wheel-test.json`. This is bounded mechanical acceptance; the page hint, full visual layout, native graphics-resource destruction and complete menu lifecycle are outside this harness.

Two earlier defects are resolved in the accepted hash:

- The original first-page identity comparison used native descending candidate order, while native `89BA4` sorts visible rows ascending by low7. Sorting the full candidate list before slicing (`job-wheel.c:candidates`) makes the first visible ID stable. The accepted pages are Human2..8 and9..12,116,117, with eligibility bit7 preserved.
- Parent's actual-emulator test found paging resumed at85C94, inside the overwritten85C90..85C97 hook bytes. `job-hooks.s:ffta_wheel_input_entry` now replays `movs r2,r0; ldr r0,[r6]` and resumes85C98. Independent installed-branch tests traverse this corrected continuation and native sorting/row construction. Literal-tail scans also reject destinations inside installed hook spans.

## Native contracts and checks

**Global job-list ABI remains12 bytes.** Native `C8A24` clears12 output bytes atC8A3E/C8A40. Its initial protected-character path returns0 atC8A70; it then enumerates original jobs43 downward. The wheel-only wrapper uses a16-byte stack candidate list and copies at most12 bytes to its caller. The test checks both buffer guards, all five races with zero/mastered AP, and all116 original character identifiers. Native identifiers9,10,94 retain zero count and a cleared output list even with all lessons mastered. The native C8C24 region and C8A24 entry were unchanged by this stage. Existing native clan-unlock side effects remain native; the tests do not require those legitimate flag writes to disappear.

**Human page writes are bounded and selections are correct.** Actual installed85C90 code runs through native85C98..85D70, including89BA4 and the28-byte wheel-row initialization. Every current Human job2..12,116,117 selects its own row on its initial page at both SP residues0/4. Count is7 or6, and the guard after the12-row area remains untouched. Two subsequent installed8616C paging passes visit all13 jobs and return. No zero/fake job ID enters the rendered row set. The native selected-current row has its eligibility/display byte cleared at85C5A; that existing selected-row treatment is preserved.

**L/R lifecycle and cleanup call contracts are correct within scope.** With native key word03000002 containing0100 or0200 and menu+25 timer0, the installed input branch calls cleanup for sprite indices0..oldCount-1, followed by1A, and asks for sound65. Native86xxx input handling reads the same key source. No key, A+L, B+L, a nonzero animation timer and nonpaged Bangaa each preserve native fallthrough registers and condition flags at86178. The harness intercepts the final sprite destructor and sound function to record their arguments; it does not claim those graphics resources were actually released in Unicorn. Parent reported complementary actual-emulator paging tests; they are separate evidence.

**Confirmation rechecks current AP.** Original native conditions at86198..861BC still precede the added hook. At861BE the helper reads selected row index/menu count, resolves the selected row's job at menu+1287+28*index, and calls the new-job predicate only for116..125. Tests keep a new row artificially marked selectable while withholding prerequisites: the installed hook follows rejection86230. With prerequisites satisfied it resumes861C8 with the same live registers/flags as the original displaced instructions. Old jobs resume that original contract regardless of the new predicate.

All ten jobs are checked against registry prerequisites using actual native learned-record type/cost data: exact required mastered counts, one action below each prerequisite threshold, and equipment-only bit80. Native AP and the entire816-byte Human sidecar region remain unchanged. The two Chemists intentionally have no prerequisites and remain eligible in all these AP fixtures; treating them as failures would be a test error. These tests use live roster ownership; they do not replace the separate party-copy AP ownership/lifecycle audit.

**Donor icon/palette selection preserves originals.** For every uint8 job input0..255 at SP residues0/4, native CB9E0's resource-decoder arguments and CBA14's returned palette match the original functions, substituting only the approved donor list for116..125:6,3,13,15,25,27,41,36,29,30. The resource decoder itself is intercepted: this proves the asset-selection and ABI contract, not pixel appearance or decompression.

**New reactions are explicitly inactive without unsafe mask reads.** Installed12E6A4 reaches `ffta_reaction_staging_guard`, which resolves the assigned lesson with nativeCD4D4. Globals128..255 return0 before the original compatibility bank. The tests execute all128 new byte values over no status, every one of44 native status bits, and all44 bits combined, at both SP residues. Every case returns0, reads none of527D5C..528A00, and leaves the264-byte unit unchanged. Original reaction0..15 is compared directly to the reconstructed baseline over the same46 status patterns; all1,472 cases agree. This guard does not implement any new reaction or authorize its eventual eligibility rules. Unassigned/unused native IDs16..127 are not being extended or certified here.

## ABI and remaining boundary

Installed wrappers preserve native stack restoration and callee-saved registers. Tests enter at native SP mod8=0 and4, measure alignment at actual C-entry addresses, and compare all live low/high registers plus NZCV for input fallthrough and successful confirmation. Actual function-return wrappers for donors/reactions are covered by the shared native ABI harness. Paging has no added persistent heap/global state; the visible page key lives in the existing wheel list.

No remaining source defect was reproduced in the accepted scope. Keep the separate actual-emulator visual/page-hint tests and full menu open/change/close tests before promoting this test ROM. This review stops native row construction before85D70 graphics work, intercepts sprite deletion/audio/icon decoding, and does not exercise full C8C24/UI story reachability or save/load after changing jobs. It does verify native protected-list return behavior rather than inferring permission from AP alone. All custom combat effects remain outside this acceptance.
