# Passing Step implementation work

The player flow, native AI route, Counter interruption and result ownership
pass within their recorded scope on `9d2e4b8ab04f2090d8e6b1ecfe9f0447a1069c5a`.
Final run `20260915T164555.393527Z` passes all55 combined steps with unchanged
inputs. Passing Step still needs applicable native law/terrain and broader
acquisition/secondary-job/campaign acceptance. This is not full-job completion.
Earlier prerequisite and candidate evidence below is retained as history.

## Current player implementation

`passing-step.c/.s` own the constructor25720, pollB6544 and main phase19
handoff93C5E. All original instructions and continuations remain available for
other actions. The builder asserts original hook bytes and reserves512 bytes
at0203E800..E9FF. Fresh-result frames occupyCE40..E7FF and all three heap
ceilings are CE40. Extra snapshot flags remain atEA00..F21F,
diagnostic scratch remainsF220..F3FF, and persistent
22-byte records retain their schema and allocation.

One exact live actor/wrapper and player controller own the transient route.
The confirmation result arms it, but only the installed snapshot's native
primary409 start and paid completion authenticate movement. Copied actors,
nested reactions and unpaid/other actions cannot authenticate it. Live
turn/battle/actor lifecycle retires the pending owner; copied lifecycle events
do not affect a live selection. The route is not serialized into unit saves.

After action playback, reject KO, Petrify, Sleep, Stop, Immobilize or actor
displacement. Rebuild native traversal from the selection origin using at
most the snapshotted remainder. The entire reconstructed path must match the
preselected coordinates, heights and jump markers. Never substitute the new
path. Native Move40/43 handles the accepted route, then its actual endpoint is
committed and the shared movement remainder is closed. A declined or rejected
route preserves ordinary Move rules; native command state never resets.

The actual action record pays6MP, uses the existing0.95Pdance finalizer and
weapon-free/Physical classification, and now has native help describing blue
tile selection and B-to-skip. Initial affected run
`20260915T154845.774871Z` passes the original-action differential,2,677 native
Dancer assertions and movement-ledger closure checks. Player script issues
were isolated separately; this is not complete Passing Step acceptance.

The eight-flow playback matrix includes three fixed attack seeds, decline,
cancel, over-budget rejection, newly occupied destination and Immobilize at
confirmation. Those last two are declared boundary inputs, not proof of
reaction-delivered effects. The player-selection manager can retain its old
actor through an AI turn, so handoff assertions must observe battle+4's live
wrapper. An enemy may subsequently damage and knock back the dancer; asserting
unchanged coordinates after that attack confuses legal gameplay with a lost
position commit. Check before the next logged attack, then independently
require return to a usable player menu.

Player-flow acceptance: assembled run `20260915T155532.814395Z` passes51/52
on17da05d; the remaining step lacked a test capture. Corrected and expanded
affected run `20260915T160906.237428Z` passes11/11 with unchanged inputs.
It contains337 player assertions over ten scenarios and a native cold resume,
plus308 callback ownership, payment, exact0.95 coefficient and hit/miss checks.
Seed0 misses and seeds3/18 hit, with the finishing movement occurring in both
outcomes. A no-approach decline retains a three-point ordinary Move; taking
the optional segment consumes Move. Pressing B after either completed step
does not undo the position or reopen movement. The ownership tests use a
captured player confirmation and the installed snapshot callbacks, including
copied actors, other actions/origins and unpaid completion controls.

The full run's other51 checks remain evidence for the same production ROM;
the final affected run is not a second full52-step run. Native reaction
delivery, movement law/trap acceptance and actual AI route choice remain open.

## Native AI route selection

Candidate `a4722af6b1db2ed3080e0b3898f400ed13e1ff1d` adds the original-byte
checked hook at93994, before the native AI sets Act flag3 and reads its
selected action throughC1514. The existing arc-facing hook at93A66 remains
untouched. Native selection must publish409 and retain the exact live wrapper,
action and successful selected node at02015488. No copied prediction owns a
route. Phase8 waits for the native asynchronous movement map; it cannot be
mistaken for post-action completion. An AI owner has no player-controller
pointer and never opens the blue-tile modal.

The AI chooses a native legal path, capped by the same remaining movement
points. Its deterministic heuristic increases Manhattan distance from the
selected target, then prefers lower terrain cost and stable Y/X order. It
does not change the native action/target scores or consume RNG. This is a
simple retreat heuristic, not a claim of optimal positioning against all
other enemies. The paid action and exact post-reaction path revalidation use
the same code as player movement.

After a completed AI step, only later Move commands in the native four-entry
command list are changed from2 to1 (native skip). Prior commands and Act/Wait
remain unchanged. This prevents a previously planned Act -> Move sequence
from adding movement after the optional segment. Decline/invalidation never
performs this rewrite. The live route remains transient and is cleared by
the ordinary turn lifecycle.

Affected run `20260915T161942.365679Z` passes14/14 with unchanged inputs.
Its three autonomous AI seeds pass57 checks: native choice of409, route before
payment, two-cost native retreat, exact committed endpoint, six MP once and
turn handoff without another Move. The actual native plan in these captures
is Move -> Act -> Wait; direct command-list boundary tests separately cover
Act -> Move and multiple future Move entries. Existing player playback,
ownership, Dancer and movement-budget tests pass on the new candidate.
Expanded AI negative cases and real Counter playback are accepted below;
applicable native law/terrain cases remain open.

Expanded run `20260915T162638.268373Z` accepts the three route seeds plus
immobilized/unlearned native AI controls and the future-command rewrite matrix.
The immobilized native selector chooses free Fight in this fixed formation;
the test preserves that choice. Separate actual409 callback checks cover Sleep,
Petrify, Immobilize and Stop without delaying the strike or creating a route.
Earlier run `162338.954393Z` incorrectly expected409 from that AI scenario and
attempted a screenshot immediately after restoring state. Both were test fixes.

Actual Counter is equipped through its native monster racial bank, with its
own native queue and damage. In `162638.268373Z`, that positive case overwrites
native renderer bytes6C8C..6D58 before playback (173 changed bytes), despite
correctly returning two result objects and50 Counter damage. This is a real
stack-budget failure, not an invalid test expectation. Candidate9d2e4b8 moves
fresh-result snapshots into a separately reserved bank with live stack-token
ownership. Its final combined regression passes below. The Counter script retains
enabled/disabled,500/1HP and seeds0/3/18 as declared inputs. Reaction damage and
KO are never injected, and code/owner guards remain enforced.

On9d2e4b8, run `20260915T164246.035067Z` accepts all12 actual Counter
scenarios with138 assertions. Surviving and lethal reaction outcomes are both
nonvacuous, and all36 native-code guards pass. The earlier full run's two new
timeouts came from one-frame confirmation presses; the fixed scripts now use
the same eight-frame press duration as the other native playback scripts.

Run `20260915T164512.554505Z` passes11/11 with unchanged inputs, including
73 AI assertions and68 external-frame ownership assertions. The AI control
permits either legal Fight or409 while immobilized, and checks the selected
command's actual MP cost. It records409 on this candidate. Neither command
may create movement. The native frame test observes Counter scopes under
both Fight and409, exact live stack tokens, independent reacting actor and
primary parent, pool/owner retirement and rejection of forged, copied,
expired and closed records. Native Fight seed18 knocks the target out of
Counter range; that fixed case correctly has no Counter and remains in the
matrix. Every actually queued Counter must own exactly one fresh frame.

Final55-step combined run `20260915T164555.393527Z` passes with unchanged
inputs on9d2e4b8. It includes337 player,73 AI,138 Counter,430 movement/ownership
and68 external-frame assertions alongside the prior job regressions and native
save/cold resumes. Root reviewed original hook replay, exact actor/route
ownership, future-Move-only rewrites, separate memory reservations and snapshot
parent restoration. Remaining acceptance is explicitly listed at the top.

## Remaining movement and law audit

Keep this scoped to the USA GBA game's actual rules. The external
[FFTA law reference](https://finalfantasy.fandom.com/wiki/Law_system_(Tactics_Advance))
and [A2 law reference](https://finalfantasy.fandom.com/wiki/Law_system_(Tactics_A2))
are different catalogs. Movement-specific A2 examples must not silently become
new FFTA restrictions. Treat the guide comparison as a research lead and verify
the applicable native table/caller before authoring acceptance scenarios.
Likewise establish actual native terrain hazards before assuming A2 trap
mechanics exist in this game. No new law/trap system is authorized here.

Static native phase table at929D4:19 ->93C5E,21 ->96908,40 ->9671C,
43 ->9680C,44 ->966E0,45 ->966FC,79 ->969A2. Phase45 clears battle
flag2000 when C95A8(13) is true; it is not by itself a law or trap dispatcher.
Phase21 calls the camera helper922CC; phase79 starts the overlay-close flow.
Within native Move43,9D614 reads movement-controller29 bit1;9D640 dispatches
board callback9C9E8 through99840.9CAF4 is a camera-follow calculation.
These are investigation pointers, not completed hazard/law acceptance. Preserve
the actual native Move40/43 traversal and action post-law handling while
establishing the remaining applicable checks.

## Native route evidence

Declared diagnostic `diagnose-passing-route` records ordinary Move/undo from
the accepted isolated Giza fixture. The fixture begins on Bangaa02000398;
fixed native Wait inputs advance to Viera020005A8. Initial failed trace
20260915T143254.585191Z caught that turn assumption. Corrected run
20260915T143342.272264Z passes both fixed routes and undo. It records ROM
hash, exact inputs, RAM/IWRAM, state and rendered frame for each stage.

The battle object is0200F4E8. Its route starts at+124, count at+1A4; the
128-byte route storage accommodates32 native4-byte nodes. Native96570 calls
9A604(wrapper,cursorX,cursorY,budget,output), delegating to989AC and1492D0.
The producer includes the origin. Routes0,14 ->0,13 and0,14 ->0,13 ->0,12
contain respectively2/3 nodes. Each node is X,Y,height,jump marker; the third
byte is not accumulated movement cost. 148A0C copies these fields.

The wrapper+3C points to the native route map. Nodes have7-byte stride;
149384 checks destination reachability and1493AC reads accumulated cost in
node byte4. The fixed map reports0 at0,14,1 at0,13 and2 at0,12. This establishes
the producer/consumer mapping; terrain-weighted and mobility-mode cases still
need explicit acceptance. The ordinary Move allowance comes fromCA394;
CA3EC multiplies it by10 and96554 divides it by10 before route construction.
Move mode/height/traps remain native. 9D464 starts the native route animation;
96790 passes the route/count, and968D3 is the observed movement-completion
flag call. Selection/no-path/Wait flag calls are not movement completion.

## Owned movement allowance

The source now snapshots the full native0..127 turn-start allowance. Existing
22-byte records and schema2/footer size remain unchanged. Byte14 low3 retains
the existing movement history; high5 stores allowance low5. Byte19 bits3..4
store allowance high2,5..6 the capped remainder0..2, and7 authenticates this
ledger. Geomancer Steady retains byte19 bits0..2. Its future readers/writers
must mask those bits and preserve the ledger. Full coordinate bytes remain.

Native completion subtracts the route map's accumulated terrain cost from
the original allowance before capping at2. Undo restores the original capped
allowance. Turn end/KO/Petrify/job/battle lifecycle clears the ledger while
preserving Steady's independent bits. Old saves without authenticated ledger
state cannot gain a finishing move until the next actual turn start. The
completed map must be ready, share the recorded turn origin and contain a
reachable destination in the native 0..15 path-grid domain. Native1CC7C alone
truncates coordinates to bytes; native97504 proves the necessary grid bounds.
Missing, stale, unfinished or zero-cost route evidence grants no remainder.

Candidate `ce21bddaa1f99e3393184e49f55e9e472f257637` passes affected run
`20260915T145951.349345Z` (11/11). The native Move/undo/AI/cold-resume replay
already passed in `20260915T145822.649105Z` on that exact ROM. That earlier
run exposed an inactive-input fixture omission: its old movement fields were
cleared, but the newly assigned high bits of19 were not. The fixture now clears
the complete domain on both inputs and preserves Geomancer's low bits, with
the full native output comparison retained. This is movement-accounting
acceptance, not acceptance of Passing Step. Full combined run
`20260915T150742.715865Z` passes all50 steps with unchanged inputs on this ROM.
The preceding full run `20260915T150034.498214Z` passed48/50; the Chemist and
Blood Edge original-action comparisons had the same inactive-input omission.
Those and the root native comparison now share
`job_test_candidate.initialize_inactive_turn_domain`, with every output byte
still compared. No production change was needed to resolve those fixtures.

`scripts/test-turn-movement-budget.py` declares cost-map inputs and exercises
the actual native map readers. It covers one fixed distance with different
costs, invalid maps, native Light Foot/gear/bonus combinations, mid-turn bonus
removal, undo, unknown ledgers and shared-byte lifecycle preservation. The
player replay independently reads native Move on a clone of each captured
state and checks the original allowance and remainder through cold suspend.

## Native confirmation handoff investigation

The final attack confirmation constructor is25720. Its only direct native
callers areB650E/B6530 in a separate target/preview controller. R8 holds that
controller; its phase is at+1118 and phase stack at+111C. Both callers advance
it to phase2. B6544 polls25464: return0 keeps waiting, -1 cancels, positive
queries254A0, with result0x12 admitting the action. The controller's pressed
and repeat input masks are retained atSP+28/SP+2C. Its normal epilogue isB7C0E.
These are static native-code findings, not proof of a working modal handoff.

The exact controller entry isB5A08, called by wrapperB7E08. Main battle phase49
at953FE passes the object at battle+60 and the two input masks toB7E08. The
constructorB7E1C allocates0x1160 bytes through22840 and initializes it viaB5278;
the ordinary action branch953A4 stores this object at battle+60. Main phase49
interprets return1 as confirmation and2 as cancellation. Confirmation95492
sets native command flag3 and callsB7E7C to release the selection object before
scheduling later action phases. A route stored only in this object would be
freed before attack execution. Its ownership therefore needs an explicit
handoff into the confirmed action's lifetime, followed by retirement on
cancellation, payment failure, actor invalidation or completed movement.

Do not confuse main phases31/32 with attack completion:93D84/93D9C manage
the unit information window. The later main phase19 turn-routing entry93C5E
is a possible deferred movement handoff, but its exact post-reaction timing
still requires a fixed native trace before installing a production hook.

The prospective route selector must preserve this chosen target/context while
using the active wrapper's post-approach coordinates. Reusing the entire Move
menu branch is unsafe: its phase29 restores the wrapper from the unit's still
uncommitted turn-start F6/F7 coordinates. Native149230 can initialize the route
map's origin and capped budget explicitly;99EA0 refreshes native traversal
parameters but supplies the old unit origin. Any use must override the origin
before99F00 starts native map processing. Do not alter saved unit coordinates
merely to preview a finishing route.

Native9A604/989AC may return a shortened route when the requested destination
fails validation. A chosen Passing Step route must end at the exact requested
tile; a positive node count alone must not silently accept this truncation.
Native nodes include jump markers and elevation. Do not replace them with
Manhattan paths or restrict the approved legal options to cardinal directions.

Still required in this batch: preselection UI including decline/cancel, native
route validation from the post-approach origin, reaction invalidation, deferred
native movement playback, one6MP/Pdance0.95 action, no new Move opportunity,
normal trap/law handling, copied AI evaluation, and consolidated fixed-script
tests. Do not enable409 as an attack-only substitute for the approved ability.

## Isolated native modal and movement proof

`scripts/diagnose-passing-modal.py`, declared as `diagnose-passing-modal` in
the integration plan, instruments the accepted production image privately.
It intercepts Forbidden Dance406 to investigate the controller handoff. It
does not enable409 or prove its damage, laws, AI or reaction interruption.
The native player fixture is the retained, hash-checked Viera start state from
`test-dancer-choice-playback`. The diagnostic uses the existing480-byte trace
reservation at0203F220 and erased code atROM13F0000; production is unchanged.

Run `20260915T153513.587772Z` passes11/11. It demonstrates native cursor
selection from the post-approach position0,13, a two-cost route to0,11, native
final confirmation, one14MP Forbidden Dance payment, native deferred Move,
the committed final position, and decline without movement. The native
IWRAM code guard remains intact. Blue reachable tiles were visually inspected
in the retained modal capture. This is an implementation prerequisite only.

The renderer requires both the native overlay constructor9A624(wrapper)
(which calls22BA4(1,2)) and repeated98A88(wrapper) until its tile-color update
finishes. The path-grid functions and9A5C0(board,1) alone do not show blue
tiles. Close the overlay through231AC and restore the saved36-byte cursor
before opening the original final-confirmation dialog. The map is initialized
with the capped remaining budget, so the native color pass cannot include
tiles beyond it even though its own allowance query reads the full Move stat.

Main phase19 at93C5E follows the paid native action in this fixed scenario.
Copy the exact native route to battle+124, its node count to+1A4 and the active
wrapper to+8; push phase19 and enter phase40. Native phases40/43 perform the
movement and return through phase19. This proves the basic handoff, not all
reaction scheduling variants. Do not assume all post-action routes are valid.

The original action has already committed the approach tile atA4438..A444C.
There is no second action after the finishing Move to commit its new tile.
On native movement completion, copy the actual wrapper coordinates to the
actor's F6/F7 exactly once. Without that commit the subsequent turn transition
can retain or drift from the approach position. Failed diagnostic run
`20260915T153308.740251Z` caught this; the successful run above includes the
commit. Never write the requested destination before native movement occurs.

Final confirmation must authenticate positive25464 and254A0()==0x12.
Cancellation retires the pending route; a nonzero poll result alone also
includes cancellation and must not arm movement. Production additionally
needs the actual action-completion/payment callback, actor ownership, route
revalidation after reactions, movement-ledger retirement, lifecycle cleanup,
AI integration and a player-facing instruction before409 can be enabled.

Expanded run `20260915T153746.061480Z` passes11/11 with unchanged inputs.
Its five fixed flows add decline-at-origin, rejection beyond the two-point
budget, and cancellation after route selection to the route/B-decline flows.
The cancellation retires the stored route and preserves the active turn,
actor position and MP. All captured selection contexts retain action406.
Instrumented ROM hash: `7de460523eb967ad677be9b48b75a766107ba78a`;
the run log embeds the complete report so later scratch captures cannot
replace the historical evidence. The original production hash is unchanged.
