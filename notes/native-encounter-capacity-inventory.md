# Native encounter capacity inventory

September18 Pacific. This is a static inventory, not E03 acceptance.
`audit-native-encounter-capacity` passes450 checks in terminal runner
20260919T000602.574074Z. Report:
`build/art/encounter-capacity-audit/20260919T000603.180786Z/report.json`.
It authenticates installed7507ca5c and clean US4ac05441, all442 original40-byte
formation records, template extents and unchanged native consumers. No runtime,
fixture, installed build or player data changes occur.

Native formations1..442 at0854CD54+40*ID have1..13 templates. Twenty-three
records have13:89,93,332,374,375,376,378,379,381,382,384,385,387,388,389,
390,391,392,393,394,395,396,397. Deployment byte+32 ranges1..8. The report
retains every record hash, template hash, allegiance flag and judge selector;
candidate template changes are explicit, not assumed absent.

The original C83A4 allocator searches exactly12 non-party264-byte unit records
starting02002FC4. This domain includes judges and allied non-party characters;
it is not twelve guaranteed enemies. The allocator's bound does not establish
the entire actor manager's maximum. The observer's permissive36-result check is
also not native capacity evidence:99CDC walks an allocator-backed object list.

Thirteen templates and twelve allocation records must be reconciled through
actual template selection, scripted spawns/retirement, party membership and
optional secondary formation consumers. Do not add template count to deployment
byte and call the sum reachable. The existing original-formation1 runtime has
13 actors total, with only two generated-class party members. It remains useful
bounded heap/Status evidence, not maximum all-ten or effect coexistence proof.

Next follow native formation construction and event scripts for the largest
records, select a demanding reachable encounter, then run targeted allocation,
palette/effect and return-lifetime acceptance. Reuse the already passing native
C83A4 exhaustion test documented in notes/exposed-storage-review.md; no need to
replay it merely to confirm the static bound. No E03 box is checked here.

## Primary scene loader bound reconciled

Static audit-current-formation-loader passes456 on current5a14e6c7 in runner
20260919T040021.192998Z. Report:
build/art/encounter-capacity-audit/20260919T040021.883005Z/report.json.
Complete native CA744..CA7A4,96E18..96E78 and123670..123694 are unchanged.
Event123670 gets the primary formation through A018, then invokes96E18. The
latter passes R2=12 and02002FC4 to CA744. That bulk loop checks the caller limit
before and after each template; with initially empty records it constructs only
the first min(count,12) entries. The audit separately retains every outside-limit
index (index12 for the13-template records), rather than treating it as allocated.

This establishes the initial loader bound, not maximum live occupancy. Its skip-
occupied loop means the empty-domain entry condition still needs lifecycle proof.
Native scripted spawn1243E2 calls C83A4, while1243C4..1243DC can reuse an existing
party/nonparty identity before allocation. Secondary roster selection A024 uses
its explicit selector or falls back to A018. These consumers and scene scripts
must be traced before declaring a maximum or choosing the final demanding map.
The existing roaming-source-access proof already establishes distinct map and
enemy formation selectors; reuse it. No new fixture or runtime was needed for
this static audit, and E03 remains open.

## Scripted, judge and saved-roster consumers

Static `audit-current-scripted-capacity` passes465 on private200ca35b, runner
20260919T060618.830982Z; report
build/art/encounter-capacity-audit/20260919T060619.567705Z/report.json.
It authenticates the added native code regions against clean US, with exact
direct-call targets and field/counter instructions. No emulator was launched.

- Scripted1243C0..12443A resolves script/native identities, checks whether an
  actor already exists, and falls back to C83A4/C9644 before92440 construction.
- Judge creation12BAA2..12BB20 reads primary formation byte33 and also uses
  C83A4/C9644/92440. Judges share the twelve nonparty records; they are not an
  extra independent allocation domain.
- Saved actor restoration992E0..993F4 walks24 party and12 nonparty records with
  native eligibility flags. This is a conservative iterator domain, not proof
  that36 actors coexist in a reachable encounter.
- The audited A024 secondary consumers123694 and123CC0 read formation metadata
  bytes38 and34 respectively. They do not invoke an additional bulk constructor.
  Other selector uses and script reachability remain separate.

The inventory now reports initial-template-plus-potential-judge counts. All
13-template records have judge selector0. Formation19 alone has twelve templates
and a nonzero judge selector, so reaching/ordering/retiring its actors needs
explicit reconciliation before a maximum-capacity claim. This is a static
selection concern, not a demonstrated overflow or a claim it is a battle map.
The primary bulk loop's occupied-slot skip is unbounded by the caller limit;
the empty-domain entry precondition remains important and unproven here.

## Event selectors narrow the formation19 concern

Static `audit-current-event-capacity` passes471 checks on private200ca35b in
terminal runner20260919T061735.618117Z. Report:
`build/art/encounter-capacity-audit/20260919T061736.385262Z/report.json`.
All256 native12-byte event records at08563A70 are unchanged. Neither primary
nor secondary fields select formation19 or29. Native script handler123898 also
searches this table (events255 down to1, matching scene byte1) and forwards the
matching primary field through9C04. This closes that additional selection path;
it does not prove absence of direct selector writes or saved-state inputs.

Formation29 is the judge-template source: native12BAB4 computes table+29*40,
loads its template pointer and selects48*(judgeSelector-1). Its four templates
cover all nonzero selectors1..4 in the442 records. It is not evidence of a
four-judge encounter. Formation19's twelve-template-plus-judge arithmetic is
therefore not a demonstrated reachable overflow. Do not make a synthetic19
replacement the final maximum-capacity encounter without further justification.

Only event-selected formations2,21,23,24,30,47 have at least nine templates.
Their event/scene links and full records are retained in the report; selection
alone does not establish a battle rather than a scene or its live occupancy.
The next E03 step is to reconcile their actual battle/script use and secondary
template consumers before choosing demanding live allocation/return coverage.
No runtime, ROM, installed game or player-save changes were made. E03 stays open.

## Actual battle loader and side correction

Static `audit-current-battle-roster` passes484 on5a14e6c7 in terminal
runner20260919T062758.672321Z (the following runtime step fails separately).
Report: `build/art/encounter-capacity-audit/20260919T062759.465563Z/report.json`.

Opcode4E/1237D0 uses1256D4 for a primary formation or124BB4 for a nonzero secondary
formation. Both iterate the full selected template count; their occupied-record
search is not a safe full-domain refusal. Both publish through124CE8/92440.
The previously audited opcode4A/96E18 twelve-template bound does not apply to
these battle constructors. Generic scenes168 and173 start0C00,4801,4E; opcode48
calls123E00, which requests a native fill of02002FC4 through02003C24 before mode
dispatch. This narrows the empty-domain concern for those exact prefixes, not
every scripted spawn or alternate scene.

Corrected inventory field: `opposingFlagCount`, replacing the erroneous
`alliedFlagCount`. Constructor C9668 transfers template+42 to unit+40; native
C8240 tests bit8000 as the opposing side. Formation32's five ordinary Giza
enemies all carry it. The earlier formation1 test had four records of each side,
so its equal counts did not expose this labeling mistake. Historical raw flags
and actual actor-count evidence remain valid; derived side counts need this fix.

The subsequent all-ten/six-versus-six runtime exposes opposing-palette support
and hardware-bank pressure rather than heap exhaustion. See
`notes/native-art-opposing-palettes.md`; no capacity gate is checked complete.
