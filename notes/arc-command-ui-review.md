# Arc list and native command review

2026-09-14. Read-only council review of the parent's isolated `combat-area.c/.s`, Earth Render103 directional metadata, and native B4A1C consumers. The parent owns production integration. The isolated `test-combat-area.py` was rerun:39,796 checks pass; binary06a950cd937dba7cef6e85ca0de5ac77ba694a11. This does not claim actual new-action execution.

The proposed three cells are geometrically correct: direction0 south,1 west,2 north,3 east matches native9D66C exactly. The center-front cell is followed by the two perpendicular flanks. No caster tile is generated. Each cell independently checks valid map coordinates, target terrain flags1/8, and inclusive height difference-2..2 relative to descriptor origin. Coordinates use descriptor+4/+5, preserving a caller's evaluated Move destination instead of rereading stale unit F6/F7. The fixed three-cell output preserves byte3 and cannot exceed native output capacity.

The B4A1C replacement's displaced twelve-byte prologue is exact through B4A26; continuationB4A28 starts `mov r6,r0`. The entry saves/restores the four native arguments and aligns the C call; nativeoriginal forwarding retains the original mode and byte-direction semantics. Native descriptors contain unit pointer+0, origin bytes+4/+5, target bytes+6/+7, action u16+8 and item u16+0A.

Native Earth Render103 yields B7C2C shapes **0x1B1 in mode0** and **0x32 in mode1**. Both select B4A1C directional branch0x30. The full native list for a flat map at(6,6), facing0, extends south to the map edge in both modes; replacing both modes with exactly three near cells is intentional. The native UI does not require an unchanged line length in this interface.

Concrete player consumer B5952..B5970 constructs this descriptor, computes target-minus-origin direction via9D6BC atB5988, and falls back to wrapper+1F only for a negative direction result. B59A0 calls B4A1C mode1. It walks the returned four-byte rows, resolves units at each tile through9961C atB59C0, and checks each unit's eligibility throughB4C90 atB59D2. B68D8/B6904 repeats the direction derivation for another native list consumer and supplies its context mode byte. Therefore list geometry alone does not prove final enemy/ally eligibility or every unit receives damage; those remain separate handler/actual-battle tests.

The apparent AI hardcoded-facing hazard is excluded by the retained donor metadata. BE7F6 passes direction0 in generic self-area evaluatorBE7AC, whose only native caller isBEF84 insideBEF28. Native selectorC02F8 reads action area selector6; areas4/6/7 route viaC0320 to **BFDB0**, notBEF28. Range selector4 bit40 independently takes the same directional route atC0310..C0320. Earth Render area6/range40 preserves that dispatch. Do not change both markers while relying on the directional evaluator.

Before acceptance, real native UI should establish: four selectable facings; selecting a diagonal flank does not unexpectedly rotate to the adjacent facing under9D6BC tie-breaking; a blocked/out-of-height center-front cell does not prevent targeting a valid flank; Move-before-Act uses the moved origin; all three units, including allies, receive separate intended effects with the caster excluded; cancel and reopen preserve AP/MP/heap; and the directional AI path evaluates all rotations. These are concrete coverage gaps, not a demonstrated implementation defect. No additional ABI or AI-routing hook is currently justified.
