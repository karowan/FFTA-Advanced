# Capacity and corrected preview delivery

Connected candidate `7507ca5cb03c1db7a717fd6d21d97a9f5a4ae08b`, following `edc7167`.
Final artwork remains deferred; built-in imagegen only.

## Larger encounter

`test-art-larger-encounter` passes 3,296 checks in runner
`20260918T212659.001298Z`, child
`build/art/connected/larger-encounter/20260918T212659.742696Z/report.json`.
The private fixture replaces exactly the complete 40-byte native formation32
record with unchanged original formation1. Its eight templates include four
native allies and four enemies, not eight enemies. The native allied ceiling
is eight: four template allies, Marche/Montblanc and DK117/Viking118. Together
with enemies and judge, 13 actual actors survive Status entry and return.
Minimum sampled free payload is20,888 bytes; largest free block20,480. Palette
refusal/ownership and heap bounds sampled every12frames pass. This is bounded
capacity through the Giza story shell, not original mission eligibility,
maximum capacity, all-ten coexistence, unsampled peaks or final art.

Failures remain retained:
- runner211559.147761Z stopped before emulation: rebuilt gameplay source path
  lacked the retained world fixture. Explicit authenticated fixtureSource is
  now separate from rebuild-source provenance; ROM bytes are unchanged.
- runners211806.682359Z and211934.484828Z expected six party units incorrectly;
  changing deployment directions did not fix the incorrect expectation.
- runner212416.433550Z captured every selection. The native 8/8 counter and
  template allegiance flags explain the actual four-party deployment.
- runner212602.572642Z reached Status and returned, then incorrectly required
  every ownership-root byte to clear. The existing destructor clears magic
  while retaining heap and balanced opened/closed counters. The passing check
  requires exactly `(0, currentBattleHeap, 1, 1)` plus retired copy ownership.
All runner dates above have prefix20260918T. No production gameplay change was
made to bypass deployment restrictions or alter the established destructor.

## Rebuild and final acceptance plan

The full art-stage rebuild in runner20260918T212659.001298Z reproduces the ROM
byte-exactly, preserving the authenticated original world fixture separately
from the clean gameplay rebuild path. Child:
`build/art/connected/full-rebuild/20260918T212707.130882Z/report.json`.

Final assembled acceptance is justified by the shared native palette-copy and
partial-damage changes, their direct menu/action consumers, and replacing a
known-broken installed technical preview. Run the bounded connected native,
portraits, equipment/menu pages, status/Status, held weapon and projectile checks,
plus current-ROM Move/cancel native phase observation. Reuse passing highlight,
damage, Thundaga, capacity and full-rebuild reports. No campaign suite or repeated
image creation is required for this delivery boundary. Package only after all
required reports and enclosing runners are terminal; final review remains open.

## Final consumer results

Runner20260918T212842.731383Z passes current-ROM frame observation5175,
portraits365, preview party8/buy21/sell21. It then stops before status emulation:
one remaining test still looked for fixture IWRAM under the rebuilt gameplay
source. Corrected to use the explicit fixtureSource. Prior passes are reused;
the failed enclosing status and log remain part of packaging provenance.

Runner20260918T213104.725389Z passes phase evidence6226, status glyphs206,
compact Status/help/return38, equipment UI30, held weapon89812, projectile4045
and native resource/import checks3981. All reports identify7507ca5c. Current
phase observation matches1024 complete ordinary-core states/frames and confirms
native phase/DMA behavior; cancellation remains4–5frames slower in this bounded
scene. Root reviewed the equipment second-page labels and the larger scene's
ready/Status/returned captures. Draft artwork and repeated poses remain unaccepted.

Packaging now requires19 current-candidate reports: final consumer/rebuild,
current frame/phase evidence, native highlight, three damage-oracle cases,
actual Thundaga and the larger-encounter fixture. The manifest distinguishes
its private formation-fixture hash from the delivered candidate hash. Packaging
and launcher validation subsequently passed as recorded below.

## Delivered checkpoint

Runner20260918T213406.602853Z passes `package-connected-art` and
`test-art-pipeline-launcher`; all enclosing runners are terminal. The package
contains19 authenticated exact-candidate reports totaling131,184 checks, plus
three launcher validation checks. Child launcher report:
`build/art/pipeline/launcher/20260918T213409.808185Z/report.json`.

- Delivery: `build/art/pipeline/delivery/7507ca5cb03c1db7a717fd6d21d97a9f5a4ae08b/`.
- ROM SHA256: `c4b8efe9bec9936788abbc4a1eb312401b0e352a4aeadc9388e7a4956efdf9a8`.
- BPS:5,584,411bytes; SHA256
  `ecc316f13a72fac4ee83a544b48175d7fd2149d5540681ca0b7ec7d5067f028a`.
- Clean source SHA1:`4ac05441f4de70a4ec3dd932116346c61b8783d9`.
- Storage: `saves/art-pipeline/7507ca5cb03c1db7a717fd6d21d97a9f5a4ae08b/`.

Patch creation is deterministic; clean-source apply is byte-exact and the wrong
source is rejected. Launcher resolves this candidate and independently overrides
savegame, savestate and screenshot paths. Every existing protected player file
matches its prevalidation hash. Nothing was launched. Prior packages are kept.

Root accepts the bounded technical transport/delivery milestone after source
review, native/current consumer evidence and visible equipment/Status review.
This is not final-art approval or a complete campaign/maximum-capacity claim.
G01–G04 remain open; the next phase is actual sprite design/animation refinement
with built-in imagegen, not external providers or hand-coded replacement art.

Final document review caught UTF-8 text being decoded with the Windows default
encoding. Corrected the source documentation before commit. The first package
and passing validation are retained. Packaging now uses a deterministic bundle
revision derived from ROM, guide, candidate and evidence identities so corrected
documentation never overwrites an earlier immutable package. Save paths remain
ROM-scoped; the corrected bundle is repackaged and revalidated separately.

Corrected documentation bundle:
`build/art/pipeline/delivery/7507ca5cb03c1db7a717fd6d21d97a9f5a4ae08b/bundle-3ea62acebcc34792/`.
Repackaging passes in runner20260918T213646.230715Z. Its launcher check fails
because the previous launcher hard-coded the ROM-only folder. The launcher now
validates the64-hex bundle ID and exact derived manifest path, preserving
checkout/link checks, ROM authentication and unchanged ROM-scoped save paths.
`test-art-pipeline-launcher` passes all three checks in terminal runner
20260918T213728.439405Z; child
`build/art/pipeline/launcher/20260918T213729.279877Z/report.json`.
No gameplay reruns were needed for documentation/package-path corrections.
