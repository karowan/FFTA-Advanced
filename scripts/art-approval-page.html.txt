<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>FFTA · Complete art approval</title>
<style>
:root{font-family:system-ui,sans-serif;color:#242b32;background:#e7e5df;--panel:#f9f7f1;--line:#d6d3c9;--accent:#285d61}*{box-sizing:border-box}body{margin:0}header,main{max-width:1420px;margin:auto;padding:28px}header{padding-bottom:12px}h1{font-size:36px;letter-spacing:-1px;margin:4px 0 12px}h2{font-size:26px;margin:4px 0 16px}h3{margin:12px 0}p{line-height:1.6;max-width:1000px}small,.muted{color:#62666b}.eyebrow{font-size:12px;letter-spacing:2px;text-transform:uppercase;color:var(--accent);font-weight:750}.toolbar{position:sticky;top:0;z-index:5;background:#f9f7f1f5;border-block:1px solid var(--line);padding:12px 28px;display:flex;flex-wrap:wrap;align-items:center;gap:10px;backdrop-filter:blur(12px)}button,select,input{font:inherit}button,select{border:1px solid #aaa99f;background:#fffef9;color:#25383a;padding:8px 12px;border-radius:6px;cursor:pointer}button:hover{background:#edf3ed}button.primary{background:var(--accent);color:white;border-color:var(--accent)}a{color:var(--accent)}nav{display:flex;flex-wrap:wrap;gap:8px;margin:18px 0}nav a{padding:7px 10px;background:#fff9;border:1px solid var(--line);border-radius:6px;text-decoration:none;font-size:13px}section.job,section.overview{padding:24px;margin-bottom:24px;background:var(--panel);border:1px solid var(--line);border-radius:12px;scroll-margin-top:130px}section.job{border-top:4px solid var(--accent)}.grid{display:flex;flex-wrap:wrap;gap:18px;align-items:start}.portrait-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(205px,1fr));gap:14px}.portrait-card{background:#eeece5;padding:16px;border:1px solid var(--line);border-radius:8px}.portrait-card h3{font-size:16px;min-height:44px;margin-top:0}.tag{display:inline-block;font-size:12px;border-radius:99px;background:#e2e6de;padding:4px 9px}.tag.bad,.issue{background:#fff0d8;color:#754812}.issue{border-left:3px solid #ba7b27;padding:12px 16px;margin:14px 0}.rowhead{display:flex;justify-content:space-between;gap:16px;flex-wrap:wrap}.figure{margin:0;background:#e8e6df;border:1px solid #dcd9d0;padding:10px;border-radius:6px;max-width:100%;scroll-margin-top:140px}figcaption{font-size:13px;line-height:1.5;margin-top:8px;max-width:290px}.pixel{image-rendering:pixelated;object-fit:contain;display:block;background:repeating-conic-gradient(#dfdfd8 0% 25%,#e9e8e1 0% 50%) 0/16px 16px}.concept{width:220px;height:270px;object-fit:contain;background:white;display:block}.screen{width:360px;max-width:100%;height:auto;image-rendering:pixelated}.stages{margin:20px 0}.stage-label{font-size:12px;color:#686d6f}details{border:1px solid var(--line);border-radius:7px;padding:12px;margin:14px 0;background:#f2f0e9}summary{cursor:pointer;font-weight:650;line-height:1.5}summary::marker{color:var(--accent)}details[open]>summary{margin-bottom:16px}.originals img{width:128px;height:128px}.sheet{width:640px;max-width:100%;image-rendering:pixelated}.sequences{display:grid;grid-template-columns:1fr;gap:16px}.sequence{background:var(--panel);border:1px solid var(--line);border-radius:8px;padding:16px;scroll-margin-top:130px}.sequence h4{margin:0 0 12px;font-size:17px}.sequence-body{display:flex;gap:18px;align-items:start;flex-wrap:wrap}.player{min-width:150px}.player img{width:128px;height:128px}.player button{padding:4px 9px;margin:8px 3px 0 0}.player output{display:block;font-size:12px;max-width:150px;margin-top:5px}.keyframes{display:flex;flex-wrap:wrap;gap:8px;flex:1;min-width:250px}.keyframe{margin:0;background:#e8e6df;border:1px solid #d7d3c8;border-radius:5px;padding:6px;scroll-margin-top:140px}.keyframe img{width:96px;height:96px}.keyframe figcaption{width:112px;font-size:11px}.control{padding:12px;background:#efeee8;border:1px dashed #aaa;max-width:132px;font-size:12px;line-height:1.5;border-radius:5px}.poses{display:grid;grid-template-columns:repeat(auto-fill,minmax(130px,1fr));gap:10px}.poses img{width:128px;height:128px}.poses figure{margin:0;padding:6px;background:#e8e6df}.badges img{margin-bottom:8px}.native-strip{display:flex;gap:15px;align-items:end;margin-top:12px}.native-strip img{width:auto;height:auto}.notice{font-size:13px;background:#edf0eb;padding:12px;border-radius:6px}.status{font-size:12px;margin-left:auto}footer{padding:24px;font-size:13px;color:#62666b}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:12px/1.5 ui-monospace,monospace}.hidden{display:none!important}.mirrored .actor{transform:scaleX(-1)}@media(max-width:700px){header,main{padding:16px}.toolbar{padding:8px;position:relative}h1{font-size:28px}section.job{padding:14px}.status{margin-left:0}.keyframes{min-width:180px}}
</style></head><body>
<header><div class="eyebrow">FFTA / Art approval / Round 01</div><h1>Every job. Every art surface.</h1>
<p>Review the artwork currently in the game. Start with the portraits and badges below, then expand each job’s animations and keyframes. Every image can be annotated in the browser. Nothing on this page is newly approved.</p>
<div class="notice">Known revisions: <b>Samurai face</b>, <b>Bangaa Dark Knight blue bandana</b>, and <b>Viera Mystic Knight portrait</b>. The Samurai’s muted-scarf sprite remains the selected base.</div>
<nav id="jump"></nav></header>
<div class="toolbar"><label>Show <select id="jobfilter"><option value="all">All ten jobs</option></select></label><button id="pause">Pause animations</button><label>Colors <select id="side"><option value="ally">Ally</option><option value="enemy">Enemy</option></select></label><label><input id="mirror" type="checkbox"> Mirror poses</label><span class="status">Annotate with Codex browser comments</span></div>
<main><section class="overview" id="portraits"><h2>Portrait approval</h2><p>These are unretouched crops from the actual job menu, enlarged 3×. Each job below also shows the concept, generated worksheet, extracted portrait, imported portrait, and battle crop.</p><div class="portrait-grid" id="portrait-overview"></div></section>
<section class="overview" id="badges"><h2>Equipment badges · all ten jobs</h2><p>Exact game-rendered lettering and head pixels, shown in the native eligible and ineligible colors. Enlarged 4×, with both at native size beneath. Color differences between these badges and battle sprites come from their separate existing native UI palettes.</p><div class="portrait-grid badges" id="badge-overview"></div><details><summary>Complete equipment screen</summary><img id="badge-screen" class="screen" alt="All ten equipment eligibility badges in game"></details></section>
<div id="jobs"></div>
<footer><a href="review-data.json">Complete review inventory</a> · <a href="export-proof.json">Pixel sources and hashes</a><p id="footer-counts"></p>Animation previews replay drawing records with their native durations at 60 ticks/second. Non-drawing control records are listed in sequence but are not simulated. These are body-art previews, not a simulation of weapon attachments, effects, movement across terrain, or battle events. Unknown action names retain their native sequence numbers.</footer></main>
<script>
const DATA=/*REVIEW_DATA*/;
const E=(tag,attrs={},text)=>{const e=document.createElement(tag);for(const[k,v]of Object.entries(attrs)){if(k==='class')e.className=v;else e.setAttribute(k,v)}if(text!==undefined)e.textContent=text;return e};
function img(src,alt,w,h,cls='pixel'){return E('img',{src,alt,width:w,height:h,class:cls,loading:'lazy'})}
function figure(src,label,w,h,cls='pixel',id){const f=E('figure',{class:'figure',...(id?{id}:{})});f.append(img(src,label,w,h,cls),E('figcaption',{},label));return f}
function detail(label){const d=E('details');d.append(E('summary',{},label));return d}
const known={0:'Walk · front',1:'Walk · rear',2:'Run · front',3:'Run · rear',4:'Flinch · front',5:'Flinch · rear',6:'Lowered stance · front',7:'Lowered stance · rear',10:'Collapsed · front',11:'Collapsed · rear',22:'Turn · front',23:'Turn · rear',64:'Crouch · front',65:'Crouch · rear',66:'Closed eyes · front',68:'Head turn · front',69:'Head turn · rear',78:'Prayer · front',79:'Prayer · rear'};
let paused=false,side='ally';const players=[];const visible=new Set();
const observer=new IntersectionObserver(entries=>entries.forEach(e=>e.isIntersecting?visible.add(e.target):visible.delete(e.target)),{rootMargin:'100px'});
function actor(pose,alt,size){const im=img(pose.images[side],alt,size,size,'pixel actor');im.dataset.ally=pose.images.ally;im.dataset.enemy=pose.images.enemy;return im}
function makeSequence(u,s,poses){const box=E('article',{class:'sequence',id:s.id});box.append(E('h4',{},`${s.lifetime==='water'?'Water':'Land'} · ${known[s.slot]||'Action sequence'} · ${String(s.slot).padStart(2,'0')}`));const body=E('div',{class:'sequence-body'}),player=E('div',{class:'player'}),frames=s.frames.filter(f=>f.command===1),rail=E('div',{class:'keyframes'});
if(frames.length){const first=frames[0],picture=actor(poses[first.pose],`${u.label} ${s.lifetime} sequence ${s.slot} animated body preview`,128),output=E('output');const state={box,picture,output,frames,poses,index:0,clock:0,paused:false};const show=()=>{const f=frames[state.index];picture.src=poses[f.pose].images[side];picture.dataset.ally=poses[f.pose].images.ally;picture.dataset.enemy=poses[f.pose].images.enemy;output.textContent=`Record ${f.index+1} · ${f.pose} · ${f.duration} ticks`};state.show=show;show();player.append(picture,output);for(const[txt,step]of[['‹',-1],['›',1]]){const b=E('button',{'aria-label':txt==='‹'?'Previous keyframe':'Next keyframe'},txt);b.onclick=()=>{state.paused=true;state.index=(state.index+step+frames.length)%frames.length;show()};player.append(b)}const play=E('button',{},'Play / stop');play.onclick=()=>{state.paused=!state.paused;state.clock=performance.now()};player.append(play);players.push(state);observer.observe(box)}
for(const f of s.frames){if(f.command===1){const k=E('figure',{class:'keyframe',id:s.id+'-record-'+f.index});k.append(actor(poses[f.pose],`${u.label} ${s.lifetime} sequence ${s.slot}, record ${f.index+1}, pose ${f.pose}`,96),E('figcaption',{},`#${f.index+1} · ${f.pose}\n${f.duration} ticks (${Math.round(f.duration/60*1000)} ms)`));rail.append(k)}else{rail.append(E('div',{class:'control'},`#${f.index+1} · Control ${f.command}\n${f.duration} ticks\nNot simulated`))}}
body.append(player,rail);box.append(body);const tech=detail('Native control and frame records');tech.append(E('pre',{},JSON.stringify(s.frames,null,2)));box.append(tech);return box}
for(const u of DATA.units){const slug=u.slug;document.querySelector('#jobfilter').append(E('option',{value:slug},u.label));document.querySelector('#jump').append(E('a',{href:'#'+slug},u.label));
const pc=E('article',{class:'portrait-card','data-job':slug});pc.append(E('h3',{},u.label),img(u.menuFace,u.label+' current in-game portrait',156,174),E('p',{class:'tag'+(u.issue?' bad':'')},u.issue?'Needs revision':'Awaiting your review'),E('p',{},u.issue||'Review face, headgear, silhouette and costume.'),E('a',{href:'#'+slug},'Compare source → game'));document.querySelector('#portrait-overview').append(pc);
const bc=E('article',{class:'portrait-card','data-job':slug});bc.append(E('h3',{},u.label),img(u.badges.eligible,u.label+' eligible equipment badge',128,64),E('small',{},'Eligible'),img(u.badges.ineligible,u.label+' ineligible equipment badge',128,64),E('small',{},'Ineligible'));const strip=E('div',{class:'native-strip'});strip.append(img(u.badges.eligible,u.label+' native eligible badge',32,16),img(u.badges.ineligible,u.label+' native ineligible badge',32,16));bc.append(strip,E('a',{href:'#'+slug+'-badge-review'},'Review this badge'));document.querySelector('#badge-overview').append(bc);
const section=E('section',{class:'job',id:slug,'data-job':slug}),head=E('div',{class:'rowhead'});head.append(E('h2',{},u.label),E('span',{class:'tag'},`${u.poses.length} poses · ${u.sequences.length} sequences`));section.append(head);if(u.issue)section.append(E('div',{class:'issue'},'Your feedback: '+u.issue));
section.append(E('h3',{},'1 · Portrait'));const stage=E('div',{class:'grid stages'});stage.append(figure(u.concept,'Original concept',220,270,'concept'),figure(u.sourcePortrait,'Generated portrait after first reduction · 64×64',192,192),figure(u.nativePortrait,'Current imported portrait · fitted within 44×54',192,192),figure(u.menuFace,'Actual job-menu face · 3×',156,174),figure(u.battleFace,'Actual battle face · 3×',156,159));section.append(stage);const native=E('div',{class:'grid native-strip'});native.append(figure(u.nativePortrait,'Imported portrait · native size',64,64),figure(u.menuFace,'Menu crop · native size',52,58),figure(u.battleFace,'Battle crop · native size',52,53));section.append(native);
const provenance=detail('How this portrait was generated · references, original output and exact prompt');provenance.append(E('p',{},'Image generation received a worksheet of original-game portraits plus the concept art. The generated worksheet was reduced, background-cleared, extracted to 64×64, then fitted again to 44×54 and quantized to 47 opaque RGB555 colors in the existing portrait format. These are the exact preserved inputs and outputs; the native result has not been visually approved by you.'));const ref=E('div',{class:'grid originals'});u.references.forEach((r,i)=>ref.append(figure(r,'Original-game reference '+(i+1),128,128)));provenance.append(ref,figure(u.template,'Exact input worksheet',640,427,'sheet'),figure(u.worksheet,'Full generated worksheet · version '+u.generationVersion,640,427,'sheet'),E('pre',{},u.prompt));section.append(provenance);
section.append(E('h3',{id:slug+'-badge-review'},'2 · Equipment badge and job-wheel figure'));const ui=E('div',{class:'grid'});ui.append(figure(u.badges.eligible,'Eligible badge · game-rendered label',192,96),figure(u.badges.ineligible,'Ineligible badge · game-rendered label',192,96),figure(u.miniatures.bright,'Job-wheel figure · bright',128,160),figure(u.miniatures.dim,'Job-wheel figure · dim',128,160));section.append(ui);
const context=detail('Full in-game context');const ctx=E('div',{class:'grid'});ctx.append(figure(u.battle,'After moving · current build',360,240,'screen'),figure(u.menu,'Portrait and job wheel · current build',360,240,'screen'));context.append(ctx);section.append(context);
section.append(E('h3',{},'3 · Animations and keyframes'));section.append(E('p',{class:'notice'},'Open a group to see every populated sequence. Each preview has previous/next controls and all keyframes beside it. Repeated poses remain listed in order. Control records are visible but not simulated; unknown meanings are labeled by sequence number. Use Pause animations before annotating a moving image.'));
const poses=Object.fromEntries(u.poses.map(p=>[p.id,p]));for(const lifetime of ['land','water']){const seqs=u.sequences.filter(s=>s.lifetime===lifetime),d=detail(`${lifetime==='land'?'Land':'Water'} animations · ${seqs.length} sequences · expand to review`);d.dataset.animations='true';let ready=false;d.addEventListener('toggle',()=>{if(d.open&&!ready){ready=true;const list=E('div',{class:'sequences'});for(const s of seqs)list.append(makeSequence(u,s,poses));d.append(list)}});section.append(d)}
const atlas=detail(`All ${u.poses.length} distinct keyframe drawings · pose index`);let atlasReady=false;atlas.addEventListener('toggle',()=>{if(atlas.open&&!atlasReady){atlasReady=true;const list=E('div',{class:'poses'});for(const p of u.poses){const f=E('figure',{id:slug+'-'+p.id});f.append(actor(p,u.label+' distinct pose '+p.id,128),E('figcaption',{},p.id+(p.meaning?' · '+p.meaning:'')));const uses=detail('Used in '+p.uses.length+' frame records');for(const use of p.uses){const link=E('a',{href:'#'+`${slug}-${use.lifetime}-${String(use.slot).padStart(2,'0')}`},`${use.lifetime} ${use.slot} / record ${use.frame+1}`);link.onclick=()=>{section.querySelectorAll('[data-animations]').forEach(x=>x.open=true)};uses.append(link,E('br'))}f.append(uses);list.append(f)}atlas.append(list)}});section.append(atlas);
const empty=detail('Unused native slots · '+u.emptySlots.length);empty.append(E('p',{},'These slots have no native sequence; they are not missing generated artwork.'),E('pre',{},u.emptySlots.map(x=>x.lifetime+' '+x.slot).join(', ')));section.append(empty);document.querySelector('#jobs').append(section)}
document.querySelector('#badge-screen').src=DATA.badgeScreenshot;document.querySelector('#footer-counts').textContent=`Build ${DATA.romSha1}. Export checked: ${DATA.counts.jobs} jobs, ${DATA.counts.poses} distinct poses, ${DATA.counts.sequences} populated sequences, ${DATA.counts.draws} draw records, ${DATA.counts.controls} control records; ${DATA.counts.emptySlots} native empty slots listed.`;
document.querySelector('#jobfilter').onchange=e=>{document.querySelectorAll('[data-job]').forEach(x=>x.classList.toggle('hidden',e.target.value!=='all'&&x.dataset.job!==e.target.value))};
document.querySelector('#pause').onclick=e=>{paused=!paused;e.target.textContent=paused?'Resume animations':'Pause animations'};
document.querySelector('#side').onchange=e=>{side=e.target.value;document.querySelectorAll('img.actor').forEach(im=>im.src=im.dataset[side])};
document.querySelector('#mirror').onchange=e=>document.body.classList.toggle('mirrored',e.target.checked);
function tick(now){if(!paused){for(const p of players){if(p.paused||!visible.has(p.box)||!p.box.getClientRects().length)continue;const duration=Math.max(1,p.frames[p.index].duration)*1000/60;if(now-p.clock>=duration){p.clock=now;p.index=(p.index+1)%p.frames.length;p.show()}}}requestAnimationFrame(tick)}requestAnimationFrame(tick);
</script></body></html>
